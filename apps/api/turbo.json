{
  "$schema": "https://turborepo.com/schema.json",
  "extends": ["//"],
  "tasks": {
    "test": {
      "inputs": [
        "$TURBO_DEFAULT$",
        "$TURBO_ROOT$/docker-compose.yml",
        "$TURBO_ROOT$/.dockerignore",
        "$TURBO_ROOT$/.env.example"
      ]
    }
  }
}
