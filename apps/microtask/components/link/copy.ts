/** The terminal page's heading, legacy's own words for a link that did not resolve. */
export const LINK_UNAVAILABLE_TITLE = 'Link unavailable'

/** The sentence under it, which is what the app being replaced showed for a revoked link. */
export const LINK_UNAVAILABLE_DETAIL = 'This share link is no longer available.'

/** What a link page's head says where overall progress would be, when there are no checklist items. */
export const SHARED_WORKSPACE = 'A shared project workspace'

/** The access badge for a link whose editor is writable. */
export const CAN_EDIT = 'You can edit'

/** The access badge for a link whose editor is not. */
export const VIEW_ONLY = 'View only'

/** A project-scoped link's list with no tasks in it. */
export const NO_SHARED_TASKS = 'No tasks have been added here yet.'
